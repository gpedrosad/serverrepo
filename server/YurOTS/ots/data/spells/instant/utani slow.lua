-- utani slow (legacy placeholder). Real Paralyze is the rune (2278 / paralyze.lua).
-- Script exists so the spell loader never crashes on missing onCast / missing file.

function onCast(cid, creaturePos, level, maglv, var)
	return false
end
