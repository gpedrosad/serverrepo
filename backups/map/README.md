# backups/map/ — copias locales de mapa / OTB

Backups y duplicados sacados de `server/YurOTS/ots/data/` para no ensuciar el árbol de mundo activo.

**No van a git** (ver `.gitignore`). El mapa canónico sigue siendo:

- `server/YurOTS/ots/data/world/test.otbm`
- `test-spawn.xml` / `test-house.xml`
